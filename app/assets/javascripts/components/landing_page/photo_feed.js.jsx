var PhotoFeed = React.createClass({
  render : function () {
    return (
      <div className="photoFeed">
        <PhotoList/>
      </div>
    );
  }
});

var PhotoList = React.createClass({


  render : function() {
    return (

    );
  }
});
